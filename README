Usage
===========
Sublime Text 2 plugin for use with CodeClimate. Currently supports:

* Cmd-Shift-C (default) launches browser and navigates to the CodeClimate analysis page of the currently open file

Installation
====================
Bring up a command line in the Packages/ folder of your Sublime user folder, and execute the following:

        git clone git://github.com/noahd1/SublimeCodeClimate.git "Code Climate"

